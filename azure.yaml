# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: angular-ssr-aca
metadata:
    template: azd-init@1.9.3
services:
    angular-ssr-aca:
        project: .
        host: containerapp
        language: ts
        dist: dist/.
        docker:
            path: Dockerfile
